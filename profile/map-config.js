// My Special Place map config — kept separate so the key can be rotated
// without touching the HTML. NOTE: this key is public by nature (it ships
// to the browser inside tile URLs); restrict it in the CARTO dashboard
// (HTTP referrer) and rotate here if it leaks.
window.MAP_CONFIG = {
  cartoKey: 'cb1_3lci_1_062278fcfaf472d518749413'
};
