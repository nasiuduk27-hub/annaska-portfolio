---
import Layout from '../../layouts/Layout.astro';
import { getMain } from '../../lib/html.js';

const main = getMain('contact/index.html');
---

<Layout active="contact">
  <Fragment set:html={main} />
</Layout>
