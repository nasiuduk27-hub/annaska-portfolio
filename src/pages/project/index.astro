---
import Layout from '../../layouts/Layout.astro';
import { getMain } from '../../lib/html.js';

const main = getMain('project/index.html');
---

<Layout active="project">
  <Fragment set:html={main} />
</Layout>
